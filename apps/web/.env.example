# ── apps/web/.env.example ────────────────────────────────────────────
# Copy to apps/web/.env and fill in. These are PUBLIC anon values (safe to
# ship to the browser). NEVER put ANTHROPIC_API_KEY or any service_role key here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
